<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:AkademiTrack.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="AkademiTrack.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="AkademiTrack"
        Background="#F8F9FA"
        MinWidth="600"
        MinHeight="500">

	<Design.DataContext>
		<vm:MainWindowViewModel/>
	</Design.DataContext>

	<Window.Styles>
		<Style Selector="Button.primary">
			<Setter Property="Background" Value="#007ACC"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="CornerRadius" Value="6"/>
			<Setter Property="Padding" Value="20,12"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="MinWidth" Value="140"/>
		</Style>

		<Style Selector="Button.primary:pointerover">
			<Setter Property="Background" Value="#005A9E"/>
		</Style>

		<Style Selector="Button.secondary">
			<Setter Property="Background" Value="#DC3545"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="CornerRadius" Value="6"/>
			<Setter Property="Padding" Value="20,12"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="MinWidth" Value="140"/>
		</Style>

		<Style Selector="Button.secondary:pointerover">
			<Setter Property="Background" Value="#B02A37"/>
		</Style>

		<Style Selector="Button.settings">
			<Setter Property="Background" Value="#6C757D"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontWeight" Value="Medium"/>
			<Setter Property="CornerRadius" Value="6"/>
			<Setter Property="Padding" Value="16,10"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="MinWidth" Value="100"/>
		</Style>

		<Style Selector="Button.settings:pointerover">
			<Setter Property="Background" Value="#545B62"/>
		</Style>

		<Style Selector="Button.cookies">
			<Setter Property="Background" Value="#28A745"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="CornerRadius" Value="6"/>
			<Setter Property="Padding" Value="16,10"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="MinWidth" Value="120"/>
		</Style>

		<Style Selector="Button.cookies:pointerover">
			<Setter Property="Background" Value="#218838"/>
		</Style>

		<Style Selector="TextBlock.title">
			<Setter Property="FontSize" Value="32"/>
			<Setter Property="FontWeight" Value="Light"/>
			<Setter Property="Foreground" Value="#212529"/>
			<Setter Property="Margin" Value="0,0,0,8"/>
		</Style>

		<Style Selector="TextBlock.subtitle">
			<Setter Property="FontSize" Value="16"/>
			<Setter Property="FontWeight" Value="Normal"/>
			<Setter Property="Foreground" Value="#6C757D"/>
			<Setter Property="Margin" Value="0,0,0,40"/>
		</Style>
	</Window.Styles>

	<Grid>
		<StackPanel HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Spacing="20">

			<!-- App Title -->
			<TextBlock Text="AkademiTrack"
                      Classes="title"
                      HorizontalAlignment="Center"/>

			<TextBlock Text="Academic Progress Automation"
                      Classes="subtitle"
                      HorizontalAlignment="Center"/>

			<!-- Main Control Buttons -->
			<StackPanel Orientation="Horizontal"
                       HorizontalAlignment="Center"
                       Spacing="16">

				<Button Content="Start Automation"
                       Classes="primary"
                       Command="{Binding StartAutomationCommand}"
                       IsEnabled="{Binding !IsAutomationRunning}"/>

				<Button Content="Stop Automation"
                       Classes="secondary"
                       Command="{Binding StopAutomationCommand}"
                       IsEnabled="{Binding IsAutomationRunning}"/>
			</StackPanel>

			<!-- Cookie and Settings Buttons -->
			<StackPanel Orientation="Horizontal"
                       HorizontalAlignment="Center"
                       Spacing="12">

				<Button Content="Get Cookies"
                       Classes="cookies"
                       Command="{Binding GetCookiesCommand}"
                       ToolTip.Tip="Extract cookies from browser after login"/>

				<Button Content="Settings"
                       Classes="settings"
                       Command="{Binding OpenSettingsCommand}"
                       ToolTip.Tip="Open cookies file location"/>
			</StackPanel>

			<!-- Status Section -->
			<Border Background="White"
                   CornerRadius="8"
                   Padding="20"
                   Margin="0,20,0,0"
                   BorderThickness="1"
                   BorderBrush="#E9ECEF"
                   MaxWidth="600">

				<StackPanel Spacing="8">
					<TextBlock Text="Status"
                              FontWeight="SemiBold"
                              FontSize="14"
                              Foreground="#495057"/>

					<TextBlock Text="{Binding StatusMessage}"
                              FontSize="14"
                              Foreground="#6C757D"
                              TextWrapping="Wrap"/>
				</StackPanel>
			</Border>

		</StackPanel>
	</Grid>
</Window>